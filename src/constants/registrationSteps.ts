import type { FormStep } from '@/components/FormStepIndicator';

export const REGISTRATION_STEPS: FormStep[] = [
  { key: 'dados', label: 'Dados pessoais' },
  { key: 'senha', label: 'Senha e acesso' },
];
